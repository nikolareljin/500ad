#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ ! -f "$ROOT_DIR/scripts/script-helpers/helpers.sh" ]]; then
  "$ROOT_DIR/scripts/update.sh"
fi

exec "$ROOT_DIR/scripts/runner.sh" "$@"
